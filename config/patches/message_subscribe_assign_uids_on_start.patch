diff --git a/message_subscribe.module b/message_subscribe.module
index f17b494..a994117 100755
--- a/message_subscribe.module
+++ b/message_subscribe.module
@@ -106,6 +106,7 @@ function message_subscribe_send_message($entity_type, $entity, Message $message,
     $context = $context ? $context : message_subscribe_get_basic_context($entity_type, $entity, $subscribe_options, $context);
 
     $subscribe_options['skip context'] = TRUE;
+    $subscribe_options['uids'] = message_subscribe_get_subscribers($entity_type, $entity, $message, $subscribe_options, $context);
 
     // Add item to the queue.
     $task = array(
@@ -258,9 +259,11 @@ function message_subscribe_get_subscribers($entity_type, $entity, Message $messa
     }
   }
 
-  // See if the author of the entity gets notified.
-  if (!$notify_message_owner && array_key_exists ($entity->uid , $uids)) {
-    unset($uids[$entity->uid]);
+    // See if the author of the entity gets notified.
+  if (!empty($entity->uid)) {
+    if (!$notify_message_owner && array_key_exists ($entity->uid , $uids)) {
+      unset($uids[$entity->uid]);
+    }
   }
 
   $accounts = user_load_multiple(array_keys($uids));
