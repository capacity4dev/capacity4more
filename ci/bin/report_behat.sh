#!/bin/sh
set -e

if [ -d "$TRAVIS_BUILD_DIR/project/profiles/capacity4more/behat/dump" ]; then
  cd $TRAVIS_BUILD_DIR/project/profiles/capacity4more/behat/dump

  # Make archive of all files in this directory.

fi