The css, fonts, images & js folders are generated by Jekyll.
Run the following (From the project root directory) in order to update the
theme's assets:

cd jekyll
npm install
bower install
grunt build
cd ..
cp -R jekyll/dist/ capacity4more/themes/c4m/kapablo/
rm capacity4more/themes/c4m/kapablo/*.html
