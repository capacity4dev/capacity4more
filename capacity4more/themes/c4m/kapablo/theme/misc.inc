<?php
/**
 * Returns HTML for a sort icon.
 *
 * @param $variables
 *   An associative array containing:
 *   - style: Set to either 'asc' or 'desc', this determines which icon to
 *     show.
 */
function kapablo_tablesort_indicator($variables) {
//  if ($variables['style'] == "asc") {
//    $element = array(
//      '#type' => 'html_tag',
//      '#tag' => 'span',
//      '#attributes' => array('class' => 'sort-asc'),
//    );
//  }
//  else {
//    $element = array(
//      '#type' => 'html_tag',
//      '#tag' => 'span',
//      '#attributes' => array('class' => 'sort-asc'),
//    );
//  }

  return '';
}